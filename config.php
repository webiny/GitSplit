<?php

/**
 * Your github account.
 */
const GIT_ACC = '';

/**
 * Master repo from which we should extract the subtree.
 */
const GIT_REPO = '';

/**
 * Path inside the master repo, where the subtree components are located.
 */
const GIT_SUBTREE = '';

/**
 * User that has commit access to GIT_ACC.
 */
const GIT_USER = '';

/**
 * Password for GIT_USER
 */
const GIT_PASS = '';